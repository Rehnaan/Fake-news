# Fake-news
Fake News Detection System is a Machine Learning application that classifies news articles as Fake or Real. It uses TF-IDF Vectorization for text processing and a Random Forest Classifier for prediction. The model is deployed using Flask with a simple web interface for user interaction.
